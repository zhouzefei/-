extends ../layout
block content
	.container
		.row
			each item in indexData
				.col-md-2
					.thumbnail
						a(href="/list/#{item.board_id}")
							img(src="#{item.img}",alt="#{item.title}")
							.caption
								h3.inLine #{item.title}